sudo: required
services:
  - docker

before_install:
  - docker pull node:8-onbuild

install:
  - docker build -t prebot-hello .

script:
  - docker run --rm prebot-hello npm run test
  - docker run --rm prebot-hello npm run lint
